# Rasterio Proximity plugin

`rasterio` plugin using `scipy.ndimage.distance_transform_edt` to produce a raster of proximity to a feature.

## Installation:

```
git clone ...
cd rio-proximity
python setup.py install
```
